# workflows/

Workflow Orchestrator가 실행하는 워크플로우 정의 폴더. (`docs/15_WORKFLOW_ORCHESTRATOR.md`)

Run Modes: manual / semi_auto / auto_until_quality / auto_until_package / full_auto
(v1.0에서는 full_auto를 기본 사용하지 않는다.)
